services:
  # Backend Service (NestJS)
  - type: web
    name: oauth-backend
    env: node
    rootDir: backend
    plan: free
    # Install dev dependencies to get Nest CLI, then build
    buildCommand: npm install --include=dev && npx prisma generate && npm run build
    # Use db push to sync schema without migrations (fixes SQLite vs Postgres conflict)
    startCommand: npx prisma db push && npm run start:prod
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: oauth-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
      - key: FRONTEND_URL
        value: https://auth-logic-playground.vercel.app
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false

databases:
  - name: oauth-db
    plan: free
    databaseName: oauth
    user: oauth_user
